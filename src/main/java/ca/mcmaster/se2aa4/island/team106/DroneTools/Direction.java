package ca.mcmaster.se2aa4.island.team106.DroneTools;

public enum Direction {
    /** North direction. */
    N, 
    /** East direction. */
    E, 
    /** South direction. */
    S,
    /** West direction. */
    W,
    /** Forward direction. */
    FORWARD,
    /** Left direction. */
    LEFT,
    /** Right direction. */
    RIGHT; 
}